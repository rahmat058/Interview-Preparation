# Fullstack ports (copy to .env)
BACKEND_PORT=5000
FRONTEND_PORT=8080

# React app path relative to repo root
FRONTEND_APP_PATH=Projects/01-autocomplete-search

# Browser calls /api → Nginx proxies to backend:5000
VITE_API_BASE_URL=/api
