# Node API port (host → container 5000)
NODE_PORT=5000

# App path relative to repository root
APP_PATH=Docker/nodejs/app

# MongoDB (only when using --profile with-db)
MONGO_PORT=27017
MONGO_URI=mongodb://mongo:27017/interview-prep
